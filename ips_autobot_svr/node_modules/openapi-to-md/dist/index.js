"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.convertMarkdown = void 0;
const swagger2openapi_1 = __importDefault(require("swagger2openapi"));
const converter_1 = require("./converter");
const renderer_1 = require("./renderer");
const utils_1 = require("./utils");
const convertMarkdown = async (src, sort = false) => {
    const document = (0, utils_1.readDocument)(src.toString());
    if (!document) {
        return undefined;
    }
    const apiDocument = (0, converter_1.createApiDocument)("openapi" in document
        ? document
        : (await swagger2openapi_1.default.convertObj(document, {})).openapi);
    if (sort) {
        apiDocument.pathMethods.sort((a, b) => a.path !== b.path
            ? a.path < b.path
                ? -1
                : 1
            : a.method < b.method
                ? -1
                : 1);
        apiDocument.references = Object.fromEntries(Object.entries(apiDocument.references).sort(([a], [b]) => a < b ? -1 : 1));
    }
    let output = (0, renderer_1.outputPathTable)(apiDocument);
    output += (0, renderer_1.outputReferenceTable)(apiDocument);
    output += (0, renderer_1.outputPathDetail)(apiDocument);
    output += (0, renderer_1.outputReferences)(apiDocument);
    output = output.trimEnd();
    return output;
};
exports.convertMarkdown = convertMarkdown;
//# sourceMappingURL=index.js.map